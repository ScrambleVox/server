'use strict';

module.exports = () => {
  console.log(`
Welcome to the scramblevox CLI toolset!

Getting started:

The command structure for the scramblevox CLI takes the form of:
$ 'scramblevox <command> <...parameters>'

To pull up this reference guide, type:
$ 'scramblevox help'
at any time.

Accepted tranform commands:

  `);
};
